name: devfest_handson
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  oktoast:
  provider:
  shared_preferences:
  flutter_secure_storage:
  http:
  flutter_launcher_icons:
  google_fonts:
  flutter_native_splash:
  flutter_spinkit:
  another_flushbar:
  flutter_svg:
  flutter_overlay_loader:
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
